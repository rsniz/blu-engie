version: '3.8'

services:
  bot:
    build: .
    image: blu-engie
    environment:
     - MONGODB_URL=mongodb://mongodb:27017/blu-engie
    ports:
      - '8085:8085'
    depends_on:
      - mongodb
    networks:
      - blu-network

  mongodb:
    image: mongo:latest
    ports:
      - '27017:27017'
    volumes:
      - ./data:/data/db
    networks:
      - blu-network

networks:
  blu-network:
    driver: bridge
